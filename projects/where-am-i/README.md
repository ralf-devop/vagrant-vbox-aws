# Where-Am-I

Python project to determine the hostname of the current system

## Installation

```
cd ~/projects/where-am-i
python3 -m venv .venv
source .venv/bin/activate
python3 -m pip install -r requirements.txt
```

## Usage

```
python3 let_the_cow_speak.py
```
